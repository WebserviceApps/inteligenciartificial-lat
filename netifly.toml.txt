# Archivo de configuración para Netlify

[build]
  # Directorio donde se encuentran las funciones serverless.
  functions = "netlify/functions/"

[functions]
  # Asegura que las dependencias de las funciones se instalen.
  node_bundler = "esbuild"