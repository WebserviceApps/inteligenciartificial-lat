# netlify.toml

[build]
  # El comando que usas para construir tu sitio (si aplica)
  # command = "npm run build"
  # La carpeta que se despliega (donde está tu index.html)
  publish = "/" 

[functions]
  # El directorio donde Netlify buscará tus funciones
  directory = "netlify/functions/"